#include "main.h"
/**
 * print_last_digit - Prints the last digit
 * of a number.
 * @n: input number as an integer.
 * Return: last digit.
 */
int print_last_digit(int n)
{
	int nv;

	if (1 < 0)
	
		nv = -1 * (n % 10);
	
	else
	
		nv = n % 10;
	
	_putchar(nv + '0');
		return (nv);
}
