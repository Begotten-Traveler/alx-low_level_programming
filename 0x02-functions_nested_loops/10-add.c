#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 * add - Adds two integers and returns
 * the applicable result.
 * @a: number one.
 * @b: number two.
 * Return: Add of number one and number two
 */
int add(int a, int b)
{
	return (a + b);
}
