#include <stdio.h>
#include "main.h"
/**
 * add - Add two integers and returns
 * the result.
 *
 * @a: number one.
 * @b: number two.
 * Return: Add of number one and number two
 */
int add(int a, int b)
{
	return (a + b);
}
